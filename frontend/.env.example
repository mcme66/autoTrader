# Base URL the SPA uses for API calls.
# Leave as "/api" so the Vite dev proxy (dev) and nginx (production) both work.
VITE_API_BASE_URL=/api

# Where the Vite dev server proxies /api during local development.
VITE_DEV_API_PROXY_TARGET=http://localhost:5088
