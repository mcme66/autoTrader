This is a folder in which varying cleaning/wrangling features/functions will be kept
Examples include:

convert_dates()

sort_rows()

convert_numeric()

remove_duplicates()

handle_missing_values()

example of a preprocessing function:
def preprocess(df):

    validate(df)

    df = clean(df)

    df = engineer_features(df)

    return df


This is the goal for the cleaning pipeline
PostgreSQL
    ↓
ingest
    ↓
validate          ← DONE
    ↓
clean             ← NEXT
    ↓
feature engineering
    ↓
model

